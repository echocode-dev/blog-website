---
import { getCollection } from "astro:content";
import TOC from "../components/TOC.astro";
import BaseLayout from "./BaseLayout.astro";
import { Calendar, CircleUserRound } from "@lucide/astro";
import { Image } from "astro:assets";
import { getRelatedPosts } from "../utils/postHelpers";

const { frontmatter, headings } = Astro.props;

const options: Intl.DateTimeFormatOptions = { year: "numeric", month: "short", day: "numeric" };

const allPosts = await getCollection("blog");
const relatedPosts = getRelatedPosts(allPosts, Astro.params.slug as string, frontmatter.tags);

const blogJsonLd = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    mainEntityOfPage: {
        "@type": "WebPage",
        "@id": Astro.url.toString(),
    },
    headline: frontmatter.title,
    description: frontmatter.summary,
    image: new URL(frontmatter.cover.src, Astro.url).toString(),
    author: {
        "@type": "Person",
        name: frontmatter.author,
    },
    datePublished: frontmatter.pubDate.toISOString(),
    dateModified: frontmatter.updatedDate ? frontmatter.updatedDate.toISOString() : frontmatter.pubDate.toISOString(),
};
---

<BaseLayout pageTitle={frontmatter.title} description={frontmatter.summary} image={frontmatter.cover}>
    <script is:inline type="application/ld+json" set:html={JSON.stringify(blogJsonLd)} />

    <main class="post-layout" data-pagefind-body>
        {
            headings && headings.length > 0 && (
                <nav class="toc-desktop">
                    <h3>Table of Content</h3>
                    <TOC headings={headings} />
                </nav>
            )
        }

        <div class="content-wrapper">
            <header>
                <h1>{frontmatter.title}</h1>
                <div class="meta-info">
                    <address class="author">
                        <CircleUserRound class="icon" />
                        <span data-pagefind-filter="author">{frontmatter.author}</span>
                    </address>
                    {
                        frontmatter.updatedDate ? (
                            <time datetime={frontmatter.updatedDate.toISOString()} class="date">
                                <Calendar class="icon" />
                                <span
                                    data-pagefind-filter={`updated_date:${frontmatter.updatedDate.toLocaleDateString("en-US", options)}`}
                                    data-pagefind-meta={`updatedDate:${frontmatter.updatedDate.toLocaleDateString("en-US", options)}`}
                                >
                                    Updated: {frontmatter.updatedDate.toLocaleDateString("en-US", options)}
                                </span>
                            </time>
                        ) : (
                            <time datetime={frontmatter.pubDate.toISOString()} class="date">
                                <Calendar class="icon" />
                                <span
                                    data-pagefind-filter={`pub_date:${frontmatter.pubDate.toLocaleDateString("en-US", options)}`}
                                    data-pagefind-meta={`pubDate:${frontmatter.pubDate.toLocaleDateString("en-US", options)}`}
                                >
                                    {frontmatter.pubDate.toLocaleDateString("en-US", options)}
                                </span>
                            </time>
                        )
                    }
                </div>
                <p class="summary">{frontmatter.summary}</p>
                <Image
                    src={frontmatter.cover}
                    layout="constrained"
                    alt={frontmatter.coverAlt}
                    inferSize={true}
                    class="cover-image"
                />
            </header>

            <div class="mobile-only-blocks">
                {
                    headings && headings.length > 0 && (
                        <nav class="toc-mobile">
                            <h3>Table of Content</h3>
                            <TOC headings={headings} />
                        </nav>
                    )
                }
                <div class="ad-block">
                    <p>AD PLACEHOLDER</p>
                </div>
            </div>

            <article>
                <slot />
            </article>
        </div>

        <aside class="sidebar">
            <div class="related-widget">
                <h3>Related posts</h3>
                <div class="related-list">
                    {
                        relatedPosts.map((post) => (
                            <div class="related-item">
                                <a href={`/posts/${post.id}/`}>{post.data.title}</a>
                            </div>
                        ))
                    }
                </div>
            </div>

            <div class="ad-block">
                <p>AD PLACEHOLDER</p>
            </div>
        </aside>

        <div id="mobile-related-content-source" style="display: none;">
            <div class="mobile-related-wrapper">
                <h3>Recent posts</h3>
                <div class="related-list">
                    {
                        relatedPosts.map((post) => (
                            <div class="related-item">
                                <a href={`/posts/${post.id}/`}>{post.data.title}</a>
                            </div>
                        ))
                    }
                </div>
            </div>
        </div>
    </main>
</BaseLayout>

<script>
    import { initTOC } from "../utils/tocObserver";

    const injectRelatedPosts = () => {
        const source = document.getElementById("mobile-related-content-source");
        const target = document.getElementById("mobile-related-posts-target");

        if (source && target) {
            target.innerHTML = source.innerHTML;
        }
    };

    document.addEventListener("astro:page-load", () => {
        injectRelatedPosts();
        initTOC();
    });
</script>

<style is:global>
    .anchor-link {
        text-decoration: none;
        opacity: 0;
        transition: opacity 0.2s;
        margin-left: 0.5rem;
    }

    @media (hover: hover) {
        .anchor:hover > .anchor-link {
            opacity: 0.8;
        }
    }
</style>

<style>
    .post-layout {
        display: flex;
        flex-direction: column;
        padding: 1rem 0.5rem;
        gap: 2rem;
        justify-content: center;
    }

    .content-wrapper {
        display: flex;
        flex-direction: column;
        padding: 1rem;
        border: 1px solid var(--text-main);
        border-radius: 12px;
        background-color: var(--bg-content);
        font-size: 1.1rem;
        height: fit-content;
    }

    .content-wrapper header,
    .content-wrapper article {
        padding: 0 1rem;
    }

    .meta-info {
        display: flex;
        gap: 1.5rem;
        color: var(--text-subtle);
        margin-bottom: 1rem;
    }

    .meta-info p,
    .meta-info span {
        margin: 0;
        font-size: 1rem;
        font-weight: 600;
    }

    .author,
    .date {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-style: normal;
    }

    .icon {
        width: 24px;
        height: 24px;
    }

    .summary {
        font-size: 1.2rem;
    }

    .cover-image {
        max-width: 100%;
        height: auto;
        border-radius: 10px;
        margin-bottom: 1.5rem;
    }

    .mobile-only-blocks {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        margin: 1.5rem 0;
    }

    .toc-mobile,
    .ad-block {
        width: 100%;
        padding: 0.5rem 1rem;
        border: 1px solid var(--text-main);
        border-radius: 12px;
        background-color: var(--bg-page);
    }

    .toc-desktop,
    .sidebar {
        display: none;
    }

    .related-list {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    @media screen and (min-width: 1200px) {
        .mobile-only-blocks {
            display: none;
        }

        .post-layout {
            flex-direction: row;
            padding: 1rem 2.5rem;
        }

        .content-wrapper {
            max-width: 60%;
            padding: 1rem 2.5rem;
        }

        .content-wrapper header,
        .content-wrapper article {
            padding: initial;
        }

        .toc-desktop {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            padding: 2rem 1rem;
            height: fit-content;
            position: sticky;
            top: 1rem;
            min-width: 20%;
            max-width: 20%;
            border: 1px solid var(--text-main);
            border-radius: 12px;
            background-color: var(--bg-content);
        }

        .sidebar {
            display: flex;
            flex-direction: column;
            min-width: 20%;
            max-width: 20%;
            flex: 1;
            gap: 2rem;
        }

        .related-widget,
        .ad-block {
            padding: 2rem 1rem;
            border: 1px solid var(--text-main);
            border-radius: 12px;
            background-color: var(--bg-content);
        }

        .toc-desktop h3,
        .related-widget h3 {
            margin: 0;
            margin-bottom: 1rem;
        }
    }
</style>
